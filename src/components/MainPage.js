import RequestSwipe from './RequestSwipe';
import UserInfo from './UserInfo';
export default function MainPage() {
    return (
        <div>
            <RequestSwipe />
            <UserInfo />
        </div>

    );
}